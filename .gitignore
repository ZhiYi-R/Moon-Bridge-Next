# ── Rust ──
/target
**/*.rs.bk

# ── 前端 ──
ui/node_modules
ui/dist
ui/*.tsbuildinfo

# ── Tauri 生成物 ──
src-tauri/gen

# ── 运行时数据（数据库 / trace / 日志）──
*.db
*.db-wal
*.db-shm

# ── 环境与系统 ──
.env
.env.*
.DS_Store

# ── IDE ──
.idea
.vscode
.qwen
# ── 外部参考项目（不入库） ──
Reference/
