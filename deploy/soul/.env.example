# 复制为 .env 后修改：
#   cp .env.example .env && chmod 600 .env
# .env 含 admin token，权限必须 600，且不得提交进版本库。

# 管理 API 与前端页面登录用的 Bearer token（必填，缺失服务会拒绝启动）。
MOONBRIDGE_ADMIN_TOKEN=please-change-me

# 容器时区。
TZ=Asia/Shanghai
